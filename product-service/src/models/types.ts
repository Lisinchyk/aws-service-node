export interface IProducts {
  id: string,
  title: string,
  count: number,
  price: number,
  description: string
}

export interface IProduct {
  title: string,
  count: number,
  price: number,
  description: string
}